<div class="top-navigation-bar">
<h2 class="entity-title"><%=t.booking_monthly_yearly_summary.title%></h2>
</div>
<div class="admin_content_wrapper" style="background-color: #f5f5f5; margin: 0">
	<div class="content">
		<div class="text-center">
			<form>
				 <select id="year" name="year">
				 </select>
			</form>
		</div>
		<br>
		<%= partial(:reservations_by_month) %>
		<%= partial(:invoicing_by_month) %>
		<% if @addon_sales_channels %>
			<%= partial(:invoicing_by_sales_channels) %>
		<% end %>
		<%= partial(:invoicing_by_product) %>
	</div>
</div>